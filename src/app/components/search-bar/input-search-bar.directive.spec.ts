import { InputSearchBarDirective } from './input-search-bar.directive';

describe('InputSearchBarDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSearchBarDirective();
    expect(directive).toBeTruthy();
  });
});
