import { ButtonSearchBarDirective } from './button-search-bar.directive';

describe('ButtonSearchBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSearchBarDirective();
    expect(directive).toBeTruthy();
  });
});
